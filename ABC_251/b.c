#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <limits.h>

int main(void)
{
	long n, w;
	scanf("%ld%ld", &n, &w);
	long a[n];

	for (int i = 0; i < n; i++)
	{
		scanf("ld", &a[i]);
	}
	
    return (0);
}
