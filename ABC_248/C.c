#include <stdio.h>
#include <stdlib.h>

int ft_roop(n, m, k)
{
	a[]
}


int main(void)
{
	int n, m, k, a[50], i;
	
	scanf("%d %d %d", &n, &m, &k);
	i = 0;
	while (i < m)
	{
		while (j < m)
		{
			while (k < m)
			{
				
			}
			
		}
		 
	}
	
}