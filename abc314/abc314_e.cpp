#include <iostream>
using namespace std;

int	main(void) {
	int	n, m, x, a, b, c, d, e, f;
	int	ans = 0;
	string	s, t;

	cin >> n >> m;
	for (int i = 0; i < n; i++) {
			cin >> c[i] >> p[i];
		for (int j = 0; j < n; j++) {
			cin >> s[i][n];
		}
	}
	return (0);
}
