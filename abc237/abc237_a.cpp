#include <iostream>
#include <climits>
using namespace std;

int	main(void) {
	long	n, m, x, a, b, c, d, e, f;
	int	ans = 0;
	string	s, t;

	cin >> n;
	if (INT_MIN <= n && n <= INT_MAX) {
		cout << "Yes" << endl;
	} else {
		cout << "No" << endl;
	}
	return (0);
}
