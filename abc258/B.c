#include <limits.h>
#include <math.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
	long n;
	long a[n][n];
	scanf("%d", &n);
	for (long i = 0; i < n; i++)
	{
		for (long j = 0; j < n; j++)
		{
			scanf("%d", &a[i][j]);
		}
	}
		



	return (0);
}