#include <iostream>
using namespace std;

int	main(void) {
	int	n, m, a, b;
	long	sum;
	string	s, t;

	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> a >> b;
		//sum = a + b;
		cout << a + b << endl;
	}
	return (0);
}
