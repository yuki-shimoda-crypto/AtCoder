#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int	main(void) {
	long	h, m, x, a, b, c, d, e, f;
	int	ans = 0;
	string	s, t;

	cin >> h;
	cout << fixed << setprecision(9) << sqrt(h * (12800000 + h)) << endl;
	return (0);
}
