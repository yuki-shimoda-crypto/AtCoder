#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main(void)
{
    int n;
    scanf("%d", &n);
    for (float i = 0; i < n; i++)
    {
        for (float j = 0; j < n; j++)
        {
            if ((i + 1) * (j + 1) )
        }
    }
printf("%f\n", sqrt(10));
printf("%f\n", sqrt(11));
printf("%f\n", sqrt(12));
printf("%f\n", sqrt(13));
printf("%f\n", sqrt(14));
printf("%f\n", sqrt(15));
printf("%f\n", sqrt(16));
printf("%f\n", sqrt(17));


    // // change
    // for (int i = 0; i < n - k; i++)
    // {
    //     if (a[i] > a[i + k] && i + k < n)
    //     {
    //         tmp = a[i];
    //         a[i] = a[i + k];
    //         a[i + k] = tmp;
    //     }
    // }
	// qsort(b, n, sizeof(int), cmpnum);
    
    // // compare
	// for (int i = 0 ; i < n; i++)
    // {
    //     if (a[i] != b[i])
    //     {
    //         flag = 0;
    //         break;
    //     }
    // }

    // if (flag)
    //     printf("Yes\n");
    // else
    //     printf("No\n");
    
    return (0);
}