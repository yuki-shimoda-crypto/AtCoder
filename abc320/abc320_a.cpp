#include <iostream>
#include <cmath>
using namespace std;

int	main(void) {
	int	n, m, x, a, b, c, d, e, f;
	int	ans = 0;
	string	s, t;

	cin >> a >> b;
	cout << (int)(pow(a, b) + pow(b, a)) << endl;
	return (0);
}
