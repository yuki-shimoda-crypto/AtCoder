#include <iostream>
#include <cmath>
using namespace std;

int	main(void) {
	int	n, m;
	int a, b, c, d, e, f;
	int	ans = 0;
	string	s, t;

	cin >> a >> b;
	cout << (int)pow(32, (a - b)) << endl;
	return (0);
}
